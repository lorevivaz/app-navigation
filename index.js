import { registerRootComponent } from 'expo';

import App from './App';

// registerRootComponent serve per registrare l'app principale 
// e garantire che l'ambiente sia configurato correttamente 

registerRootComponent(App);

